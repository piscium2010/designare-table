import React from 'react'
export const Context = React.createContext({ columns: [], data: [] })
export const TheadContext = React.createContext({})
export const ThsContext = React.createContext({})
// export const ThContext = React.createContext({})
export const TBodyContext = React.createContext({})
export const TdsContext = React.createContext({})